import React from "react";
import { View, StyleSheet, Text } from "react-native";

const LoginScreen = () => {
  return <Text style={styles.textStyle}> LoginScreen </Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 48
  }
});

export default LoginScreen;

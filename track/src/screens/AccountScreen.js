import React from "react";
import { View, StyleSheet, Text } from "react-native";

const AccountScreen = () => {
  return <Text style={styles.textStyle}> AccountScreen </Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 48
  }
});

export default AccountScreen;
